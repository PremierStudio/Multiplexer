[package]
name = "multiplexer-worktree"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Multiplexer git worktree list --porcelain parser."

[dependencies]
thiserror.workspace = true

[dev-dependencies]
proptest.workspace = true