[package]
name = "multiplexer-core"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Session runtime composition: provider + resource manager + checkpoints."

[dependencies]
multiplexer-checkpoint = { path = "../multiplexer-checkpoint" }
multiplexer-provider = { path = "../multiplexer-provider" }
multiplexer-resman = { path = "../multiplexer-resman" }
thiserror.workspace = true

[dev-dependencies]
proptest.workspace = true
