[package]
name = "multiplexer-checkpoint"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Hidden-git checkpoint store: capture, restore, and diff against refs/multiplexer/checkpoints."

[dependencies]
thiserror.workspace = true

[dev-dependencies]
proptest.workspace = true
