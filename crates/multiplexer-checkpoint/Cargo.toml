[package]
name = "multiplexer-checkpoint"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "In-memory hidden-ref checkpoint store (Phase 1.8 stub; no real git)."

[dependencies]
thiserror.workspace = true

[dev-dependencies]
proptest.workspace = true
