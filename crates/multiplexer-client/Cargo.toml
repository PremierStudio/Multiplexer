[package]
name = "multiplexer-client"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Non-blocking grok -p turn jobs so the desktop UI thread never waits on the CLI."

[dependencies]
thiserror.workspace = true
