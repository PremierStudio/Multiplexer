[package]
name = "multiplexer-terminal"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "In-memory terminal hub (PTY comes later): ids, spec, create/list/resize/input/kill."

[dependencies]
thiserror.workspace = true

[dev-dependencies]
proptest.workspace = true
