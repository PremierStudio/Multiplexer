[package]
name = "multiplexer-terminal"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "In-memory terminal hub plus piped process capture (PTY comes later)."

[dependencies]
thiserror.workspace = true

[dev-dependencies]
proptest.workspace = true
