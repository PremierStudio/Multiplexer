[package]
name = "multiplexer-mcp"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Pure MCP lifecycle state machine: reuse by config hash, refcount, crash backoff."

[dependencies]
thiserror.workspace = true
toml.workspace = true

[dev-dependencies]
proptest.workspace = true
