[package]
name = "multiplexer-shell"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Pure desktop chrome: window title plus Outlook layout. No GPUI types."

[dependencies]
multiplexer-layout = { path = "../multiplexer-layout" }
