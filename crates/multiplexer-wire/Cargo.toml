[package]
name = "multiplexer-wire"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Multiplexer shared wire contract: the single source of truth for the JSON-RPC contract (D13/D20)."

[dependencies]
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true

[dev-dependencies]
proptest.workspace = true